#!/usr/bin/env bash

set -E

MOPE_VERSION="$1"
if [[ -z "$MOPE_VERSION" ]]; then
	echo "usage: mope set-default VERSION" >&2
	exit 1
fi

# Make sure the specified version is installed
mope-path "$MOPE_VERSION" >/dev/null

echo "$MOPE_VERSION" > "${HOME}/.mope/default"